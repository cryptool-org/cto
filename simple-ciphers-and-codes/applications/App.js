// über gulp-include kommt der plugin name
// der wird über const { default as Algorhtm } = import(PLUGINNAME)
// man kann auch noch über const { default as ChildComponent } ne eigene component machen
// die props werden über <ChildComponent {...this.props} /> weiter gegeben

// wo wird das alphabet gespeichert?
// am besten in den props :D wie kommen wir an die default-gui? wie wird die eingebunden?

// ok. also in der App wird die DefaultGUI normal eingebunden
// die DefaultGUI hat props oder state oder so.



// beides kombinieren?
